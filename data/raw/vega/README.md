# VEGA QSAR raw cache

VEGA QSAR 1.2.6 was assessed on 2026-09-22. The current host has no Java runtime; VEGA requires Java 17 or newer and first-run component downloads. This module is therefore `BLOCKED`, not simulated. Place an official batch export at `vega_predictions.csv` after the runtime and model bundle are installed.
