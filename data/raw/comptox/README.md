# EPA CompTox cache

Place an official API or batch export in this directory as `comptox_fate.csv`. Record measured versus predicted status, model name, units, confidence or applicability-domain information, source URL and access date.

API keys must be read from `CTX_API_KEY` and must never be committed.

