# ADMETlab 3.0 cache

Place the unmodified batch export in this directory as `admetlab_predictions.csv`. Required identifying fields are `compound_id` and `endpoint`; retain original prediction, probability, uncertainty and applicability-domain fields whenever the export provides them.

The pipeline never calls ADMETlab automatically. Manual or documented batch exports must be preserved here with access date and model version.

